import React from 'react'
import { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import axios from 'axios'

const Transfer = ({
  users,
  amount,
  setAmount,
  userInfo,
  receiver,
  setReciever,
  setUsers,
  setUserInfo,
  setAllUsers,
}) => {
  return (
    <>
      <h1>transfer testing</h1>
    </>
  )
}

export default Transfer
